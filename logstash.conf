input {
  tcp {
    port => 5044
    codec => json
  }
}

filter {
  # Add filters if needed, e.g., parsing or enriching logs
}

output {
  stdout {
    codec => rubydebug
  }
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "django-logs-%{+YYYY.MM.dd}"
  }
}